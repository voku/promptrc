import React from 'react';

// Content has been migrated to the main pattern library as "Legacy Rules".
// This component is kept to prevent import errors but renders nothing.
export const Principles: React.FC = () => {
  return null;
};